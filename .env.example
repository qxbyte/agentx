# 数据库
AGENTX_DB_USER=agentx
AGENTX_DB_PASSWORD=agentx
# JWT 签名密钥（生产必须改，>=32 字符随机串）
AGENTX_JWT_SECRET=
# api-key 加密主密钥：openssl rand -base64 32
AGENTX_MASTER_KEY=
# 初始管理员密码（首启种子）
AGENTX_ADMIN_PASSWORD=admin123
# 向量维度（须与 embedding 模型一致；bge-m3/通义 text-embedding-v4 为 1024）
AGENTX_VECTOR_DIM=1024
