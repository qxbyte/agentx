spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agentx_test
    username: agentx
    password: agentx
  flyway:
    clean-disabled: false
agentx:
  skills:
    root: ${java.io.tmpdir}/agentx-test-skills
  plugins:
    root: ${java.io.tmpdir}/agentx-test-plugins
